/* ==========================================================================
   RAKSHAKAI — AI DISASTER RESPONSE SYSTEM DESIGN SYSTEM
   Dark Tactical HUD & Premium Glassmorphism Theme
   ========================================================================== */
@import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800;900&family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500;700&display=swap');
:root {
  /* Color Palette - HSL Tailored Dark Theme */
  --bg-dark: #070913;
  --bg-card: rgba(15, 23, 42, 0.75);
  --bg-card-hover: rgba(30, 41, 59, 0.85);
  --bg-panel: rgba(11, 17, 32, 0.85);
  --glass-border: rgba(255, 255, 255, 0.08);
  --glass-border-bright: rgba(56, 189, 248, 0.3);
  /* Tactical Accents */
  --accent-primary: #38bdf8;       /* Sky Blue Cyan */
  --accent-primary-glow: rgba(56, 189, 248, 0.35);
  --accent-critical: #ff3b5c;      /* Neon Red */
  --accent-critical-glow: rgba(255, 59, 92, 0.4);
  --accent-high: #ff8800;          /* Warning Amber */
  --accent-high-glow: rgba(255, 136, 0, 0.35);
  --accent-medium: #eab308;        /* Yellow */
  --accent-low: #10b981;           /* Neon Emerald Green */
  --accent-low-glow: rgba(16, 185, 129, 0.35);
  --accent-indigo: #6366f1;        /* AI Purple/Indigo */
  --accent-indigo-glow: rgba(99, 102, 241, 0.35);
  /* Text Colors */
  --text-main: #f8fafc;
  --text-muted: #94a3b8;
  --text-dim: #64748b;
  
  /* Typography */
  --font-heading: 'Outfit', sans-serif;
  --font-body: 'Inter', sans-serif;
  --font-mono: 'JetBrains Mono', monospace;
  /* Shadows & Blur */
  --backdrop-blur: blur(16px);
  --box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.4);
  --glow-shadow: 0 0 25px var(--accent-primary-glow);
  --radius-sm: 8px;
  --radius-md: 14px;
  --radius-lg: 20px;
  --radius-xl: 28px;
}
/* Reset & Base Setup */
*, *::before, *::after {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}
html, body {
  width: 100%;
  min-height: 100vh;
  background-color: var(--bg-dark);
  color: var(--text-main);
  font-family: var(--font-body);
  overflow-x: hidden;
  line-height: 1.5;
  background-image: 
    radial-gradient(circle at 15% 15%, rgba(56, 189, 248, 0.08) 0%, transparent 40%),
    radial-gradient(circle at 85% 85%, rgba(99, 102, 241, 0.08) 0%, transparent 40%),
    radial-gradient(circle at 50% 50%, rgba(255, 59, 92, 0.04) 0%, transparent 60%);
  background-attachment: fixed;
}
/* Scrollbar Styling */
::-webkit-scrollbar {
  width: 6px;
  height: 6px;
}
::-webkit-scrollbar-track {
  background: rgba(15, 23, 42, 0.6);
}
::-webkit-scrollbar-thumb {
  background: rgba(56, 189, 248, 0.3);
  border-radius: 4px;
}
::-webkit-scrollbar-thumb:hover {
  background: rgba(56, 189, 248, 0.6);
}
/* Navigation Header */
.navbar {
  position: sticky;
  top: 0;
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1rem 2rem;
  background: rgba(7, 9, 19, 0.85);
  backdrop-filter: var(--backdrop-blur);
  border-bottom: 1px solid var(--glass-border);
}
.brand-logo {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  text-decoration: none;
}
.shield-icon {
  width: 38px;
  height: 38px;
  background: linear-gradient(135deg, #ff3b5c 0%, #6366f1 100%);
  border-radius: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.3rem;
  box-shadow: 0 0 15px rgba(255, 59, 92, 0.5);
  animation: pulse-glow 3s infinite alternate;
}
.brand-name {
  font-family: var(--font-heading);
  font-weight: 800;
  font-size: 1.4rem;
  letter-spacing: 0.5px;
  color: #fff;
}
.brand-name span {
  color: var(--accent-primary);
}
.nav-links {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  list-style: none;
}
.nav-item a {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.55rem 1.1rem;
  color: var(--text-muted);
  text-decoration: none;
  font-family: var(--font-heading);
  font-weight: 600;
  font-size: 0.92rem;
  border-radius: var(--radius-sm);
  transition: all 0.25s ease;
  border: 1px solid transparent;
}
.nav-item a:hover {
  color: #fff;
  background: rgba(255, 255, 255, 0.05);
  border-color: rgba(255, 255, 255, 0.1);
}
.nav-item a.active {
  color: var(--accent-primary);
  background: rgba(56, 189, 248, 0.12);
  border-color: rgba(56, 189, 248, 0.3);
  box-shadow: 0 0 12px rgba(56, 189, 248, 0.15);
}
.header-actions {
  display: flex;
  align-items: center;
  gap: 1rem;
}
.badge-live {
  display: inline-flex;
  align-items: center;
  gap: 0.4rem;
  padding: 0.35rem 0.75rem;
  background: rgba(255, 59, 92, 0.15);
  border: 1px solid rgba(255, 59, 92, 0.4);
  border-radius: 50px;
  color: var(--accent-critical);
  font-family: var(--font-mono);
  font-size: 0.75rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 1px;
}
.live-dot {
  width: 8px;
  height: 8px;
  background: var(--accent-critical);
  border-radius: 50%;
  animation: blink 1.2s infinite;
}
.btn-judge-mode {
  background: linear-gradient(135deg, #6366f1, #8b5cf6);
  color: #fff;
  border: none;
  padding: 0.6rem 1.2rem;
  border-radius: var(--radius-sm);
  font-family: var(--font-heading);
  font-weight: 700;
  font-size: 0.88rem;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 0.5rem;
  box-shadow: 0 0 15px rgba(99, 102, 241, 0.4);
  transition: all 0.25s ease;
}
.btn-judge-mode:hover {
  transform: translateY(-2px);
  box-shadow: 0 0 25px rgba(139, 92, 246, 0.6);
}
.btn-sos-trigger {
  background: linear-gradient(135deg, #ff3b5c, #e11d48);
  color: #fff;
  border: none;
  padding: 0.6rem 1.2rem;
  border-radius: var(--radius-sm);
  font-family: var(--font-heading);
  font-weight: 800;
  font-size: 0.9rem;
  cursor: pointer;
  display: flex;
  align-items: center;
  gap: 0.4rem;
  box-shadow: 0 0 20px rgba(255, 59, 92, 0.6);
  animation: sos-pulse 2s infinite;
}
.btn-sos-trigger:hover {
  transform: scale(1.04);
}
/* Glass Card & UI Containers */
.glass-card {
  background: var(--bg-card);
  backdrop-filter: var(--backdrop-blur);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-md);
  padding: 1.5rem;
  box-shadow: var(--box-shadow);
  transition: all 0.3s cubic-bezier(0.16, 1, 0.3, 1);
  position: relative;
  overflow: hidden;
}
.glass-card:hover {
  border-color: var(--glass-border-bright);
}
/* Ticker Bar */
.ticker-bar {
  background: rgba(15, 23, 42, 0.9);
  border-bottom: 1px solid var(--glass-border);
  padding: 0.4rem 2rem;
  display: flex;
  align-items: center;
  gap: 1rem;
  font-family: var(--font-mono);
  font-size: 0.8rem;
  color: var(--text-muted);
  overflow: hidden;
}
.ticker-label {
  background: var(--accent-critical);
  color: #fff;
  padding: 0.1rem 0.5rem;
  border-radius: 4px;
  font-weight: 700;
  font-size: 0.7rem;
  white-space: nowrap;
}
.ticker-content {
  display: flex;
  gap: 2rem;
  animation: ticker-slide 30s linear infinite;
  white-space: nowrap;
}
/* Common Modal Styles */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  background: rgba(4, 7, 15, 0.85);
  backdrop-filter: blur(12px);
  z-index: 2000;
  display: flex;
  align-items: center;
  justify-content: center;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.3s ease;
}
.modal-overlay.active {
  opacity: 1;
  pointer-events: auto;
}
.modal-box {
  background: rgba(15, 23, 42, 0.95);
  border: 1px solid var(--accent-primary-glow);
  border-radius: var(--radius-lg);
  padding: 2rem;
  width: 90%;
  max-width: 720px;
  max-height: 85vh;
  overflow-y: auto;
  box-shadow: 0 0 40px rgba(56, 189, 248, 0.2);
  transform: translateY(20px) scale(0.96);
  transition: all 0.3s ease;
}
.modal-overlay.active .modal-box {
  transform: translateY(0) scale(1);
}
.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 1.5rem;
  padding-bottom: 1rem;
  border-bottom: 1px solid var(--glass-border);
}
.modal-title {
  font-family: var(--font-heading);
  font-size: 1.3rem;
  font-weight: 700;
  color: #fff;
  display: flex;
  align-items: center;
  gap: 0.5rem;
}
.btn-close-modal {
  background: none;
  border: none;
  color: var(--text-muted);
  font-size: 1.5rem;
  cursor: pointer;
  transition: color 0.2s;
}
.btn-close-modal:hover {
  color: var(--accent-critical);
}
/* Toast Notifications */
.toast-container {
  position: fixed;
  bottom: 2rem;
  right: 2rem;
  z-index: 3000;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}
.toast {
  background: rgba(15, 23, 42, 0.95);
  border-left: 4px solid var(--accent-primary);
  border: 1px solid var(--glass-border);
  padding: 0.9rem 1.4rem;
  border-radius: var(--radius-sm);
  color: #fff;
  font-size: 0.88rem;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
  display: flex;
  align-items: center;
  gap: 0.75rem;
  animation: slide-in-right 0.3s ease forwards;
}
.toast.critical {
  border-left-color: var(--accent-critical);
  box-shadow: 0 0 20px rgba(255, 59, 92, 0.3);
}
.toast.success {
  border-left-color: var(--accent-low);
}
/* Keyframe Animations */
@keyframes blink {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.3; }
}
@keyframes pulse-glow {
  0% { box-shadow: 0 0 15px rgba(255, 59, 92, 0.4); }
  100% { box-shadow: 0 0 25px rgba(99, 102, 241, 0.7); }
}
@keyframes sos-pulse {
  0% { box-shadow: 0 0 0 0 rgba(255, 59, 92, 0.7); }
  70% { box-shadow: 0 0 0 14px rgba(255, 59, 92, 0); }
  100% { box-shadow: 0 0 0 0 rgba(255, 59, 92, 0); }
}
@keyframes ticker-slide {
  0% { transform: translateX(0); }
  100% { transform: translateX(-50%); }
}
@keyframes slide-in-right {
  from { transform: translateX(100%); opacity: 0; }
  to { transform: translateX(0); opacity: 1; }
}
/* Utility Badges & Tags */
.tag {
  display: inline-flex;
  align-items: center;
  gap: 0.3rem;
  padding: 0.2rem 0.55rem;
  border-radius: 4px;
  font-size: 0.75rem;
  font-weight: 700;
  font-family: var(--font-mono);
}
.tag-critical {
  background: rgba(255, 59, 92, 0.15);
  color: var(--accent-critical);
  border: 1px solid rgba(255, 59, 92, 0.3);
}
.tag-high {
  background: rgba(255, 136, 0, 0.15);
  color: var(--accent-high);
  border: 1px solid rgba(255, 136, 0, 0.3);
}
.tag-low {
  background: rgba(16, 185, 129, 0.15);
  color: var(--accent-low);
  border: 1px solid rgba(16, 185, 129, 0.3);
}
.tag-ai {
  background: rgba(99, 102, 241, 0.15);
  color: #a5b4fc;
  border: 1px solid rgba(99, 102, 241, 0.3);
}
/* General Layout Containers */
.container {
  max-width: 1320px;
  margin: 0 auto;
  padding: 2rem 1.5rem;
}
.footer {
  border-top: 1px solid var(--glass-border);
  padding: 2rem;
  text-align: center;
  color: var(--text-dim);
  font-size: 0.85rem;
  margin-top: 4rem;
  background: rgba(7, 9, 19, 0.9);
}
.footer span {
  color: var(--accent-primary);
  font-weight: 600;
}
